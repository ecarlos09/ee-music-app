// Import screen watcher
import { render, screen, within } from '@testing-library/react';

// Import our app file
import Singles from '.';

describe('Header', () => {
    beforeEach(() => {
        // Render the Singles component before each test
        render(<Singles />);
    })

    it('renders ...', () => {

    })
})